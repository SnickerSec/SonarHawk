{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "20"
  },
  "build": {
    "command": "npm install && cd web && npm install @rollup/rollup-linux-x64-gnu --no-save && cd .. && npm run build"
  },
  "deploy": {
    "startCommand": "cd web && NODE_ENV=production node server.js"
  }
}
