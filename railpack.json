{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "20"
  },
  "build": {
    "command": "npm install && npm install @rollup/rollup-linux-x64-gnu --save-optional && npm run build"
  },
  "deploy": {
    "startCommand": "cd web && NODE_ENV=production node server.js"
  }
}
